server:
  port: 8086

spring:
  application:
    name: notification-service
  rabbitmq:
    host: rabbitmq
    port: 5672

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/
